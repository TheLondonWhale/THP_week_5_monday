<h1 align="center"> Bienvenue <%= @first_name %> ! </h1>

<br>

<p>Ici c'est notre super site de potins, il est chouette, non ?</p>

<p> Voici la liste des derniers potins: </p>

<% Gossip.all.each do |gossip| %>

<div class="card border-light mb-3" style="max-width: 18rem;">
  <div class="card-header">Le gossip n°<%= gossip.id%></div>
  <div class="card-body">
    <h5 class="card-title"><i><%= gossip.author %> </i></h5>
    <p class="card-text"><%= gossip.content%> <br> Pour voir le gossip <%= link_to "Clique ici", gossip_path(gossip.id) %> </div>
</div>

<% end %>
